{
  "name": "spacehuggers-firetv",
  "version": "1.0.124",
  "description": "Space Huggers - Fire TV port via Capacitor",
  "private": true,
  "type": "module",
  "scripts": {
    "sync": "cap sync",
    "sync:source": "bash scripts/sync-www.sh",
    "check:www": "bash scripts/sync-www.sh --check",
    "parse:www": "node scripts/parse-www.js",
    "open:android": "cap open android",
    "build:android": "npm run check:www && npm run parse:www && npm run sync:source && cap sync && cd android && ./gradlew assembleDebug",
    "prereqs": "bash scripts/check-prereqs.sh",
    "version:increment": "node scripts/increment-version.js",
    "build": "echo \"No bundler step required - assets are already in www/\" && node -e \"const fs=require('fs');for(const f of ['www/index.html','www/tiles.png','www/app.js','www/engine/engine.js']){try{fs.accessSync(f)}catch(e){console.error('Missing '+f);process.exit(1)}}console.log('www/ assets present')\"",
    "deploy": "bash scripts/build-and-deploy.sh"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
